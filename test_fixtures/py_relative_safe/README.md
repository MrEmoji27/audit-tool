# Relative Safe
Uses `../config/settings.json` from `src/app.py`. This stays inside the project root and should PASS Check 2.


## Installation

pip install -r requirements.txt

## Run

python main.py
