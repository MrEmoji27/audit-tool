# py_with_env_vars

A fixture project that uses environment variables but provides no `.env.example` file.

## Setup

No dependencies required — stdlib only.

## Run

```
python main.py
```

Set `API_KEY` and `SECRET_TOKEN` environment variables before running.
